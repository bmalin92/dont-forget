import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default class NotificationCategory extends Component {
  render() {
    return (
      <View>
        <Text>This is a notification category</Text>
      </View>
    );
  }
}